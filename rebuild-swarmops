#!/usr/bin/env bash
cd "$(dirname "$0")"
if make restart > /dev/null 2>&1 && systemctl --user restart swarmops > /dev/null 2>&1; then
    echo "swarmops rebuilt and restarted successfully"
else
    echo "swarmops rebuild/restart failed" >&2
    exit 1
fi
